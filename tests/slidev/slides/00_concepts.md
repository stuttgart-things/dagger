---
layout: default
---

<div class="page-label">00 · Test Concepts</div>

# A styled <span class="accent">partial</span><span class="dot">.</span>

<p class="lede">
Loaded via <code>src:</code> include, demonstrates the <code>--extras</code> flag.
</p>

| Module function | What it does                                  |
|-----------------|-----------------------------------------------|
| `init-deck`     | Scaffolds the deck from `slides.md`           |
| `build`         | Builds a static `dist/`                       |
| `serve`         | Runs the dev server with hot reload           |
| `export`        | Renders a PDF / PPTX / PNGs via Chromium      |

<div class="slide-meta">
  <span>00 / Concepts</span>
  <span>dagger slidev test fixture</span>
</div>
